
Types of CSS:
[Wher do we write our CSS]

1. Inline
     => It is written along with html  codes, as html attribute.
     => not a good practice
     => do not need selecter
2. Internal.
     => Part of HTML file.
     => Not a good practice
3. External
     => Separate CSS file.


CSS Selector:
     => Help you seslect  element/elements where styles need to be applied.

     ID
         => Apply unique styles. Can be applied to just on control.
         
     TagNAme => Applies style to all the matching tags

     Class => A custom CSS configuration to appliy on different elements.

     * => It applies to all elements

     
Background-color